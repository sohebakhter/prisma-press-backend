enum ActiveStatus {
  ACTIVE
  BLOCKED
}

enum Role {
  ADMIN
  USER
  AUTHOR
}

enum PostStatus {
  DRAFT
  PUBLISHED
  ARCHIVED
}

enum CommentStatus {
  APPROVED
  REJECT
}

enum SubscriptionStatus {
  ACTIVE
  CANCELLED
  EXPIRED
}
